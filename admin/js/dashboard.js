/**
 * Abilities Bridge - Dashboard Widget Scripts
 *
 * @package Abilities_Bridge
 */

(function($) {
	'use strict';

	/**
	 * Initialize dashboard
	 */
	function init() {
		// NOTE: Conversation management is handled by admin-app-simple.js
		// Dashboard now only contains static information cards
	}

	// Initialize on document ready
	$(document).ready(init);

})(jQuery);
